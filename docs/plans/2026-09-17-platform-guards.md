# 平台能力与认证代理安全实施清单

范围：只落实已确认的平台能力开关与可信代理认证限流。
保留 Better Auth、个人空间、RLS、模拟调度和客户端导航，不接入真实 Agent、支付或监控设施。

## 实施与验收

- [x] 阅读现有实现并运行基线：后端 77 通过、2 项集成跳过，前端 4 通过。
- [x] 先补平台模式禁用写操作、启动失败、代理防伪造测试，确认失败。
- [x] 默认平台模式：Better Auth + PostgreSQL；旧 Agent 与模拟充值仅非生产 legacy 模式显式开启。
- [x] API 返回能力状态，前端加载失败时保持操作关闭，保留查询与资源库。
- [x] 平台模式启动 API/控制台，不启动或消费旧 Agent Worker/Runtime。
- [x] 显式代理 IP/CIDR 白名单；验证转发链并覆盖内部 IP 头；沿用 Better Auth 数据库限流。
- [x] 验证临时 PostgreSQL 下双 API 共享限流、并发上限和账户隔离。
- [x] 回归调度 10 项、平台专项 15 项、后端 88 项通过（专用集成 2 项由独立脚本覆盖）、前端 9 项通过、前端构建通过。
- [x] Swarm 验收通过：报告 bairui-swarm-check-eef00244e1，覆盖可信代理、双 API 共享限流、独立真实客户端与 Worker 故障恢复。
- [x] 浏览器桌面/移动端验证原导航、资源编辑与收藏、禁用操作、配置失败、乱序响应、退出及换号隔离。
- [x] 新增中文 docs/36 操作与边界说明，同步文档索引、AGENTS 和历史文档适用范围。

## 环境保护

不读取、修改业务 `.env`，不连接 `bairui`，不变更现有数据库容器。
数据库验证使用新建且自动清理的测试容器。现有工作区改动保留；PowerShell 脚本保留 UTF-8 BOM。
