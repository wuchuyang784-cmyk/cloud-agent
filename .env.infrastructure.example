# 独立采集配置。复制为 .env.infrastructure；不要复用应用账号或业务 .env。
# 先由 DBA 执行 035，并为专用采集账号配置口令和绑定采集源。
BAIRUI_INFRA_DATABASE_URL=
# 仅启用本机只读 Docker CLI 查询，不执行部署、重启或扩容。
BAIRUI_INFRA_SWARM=1
# 本机 Windows Docker Desktop 通常为 desktop-linux；其他机器使用自己的本地 context。
BAIRUI_INFRA_DOCKER_CONTEXT=desktop-linux
